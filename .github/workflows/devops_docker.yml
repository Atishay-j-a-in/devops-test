name: Deploy node
on:
  push:
    branches: [ main ]
jobs:
    build:
        runs-on: ubuntu-latest

        steps:
        - name: checkout code
          uses: actions/checkout@v4
        
        - name: Login to Docker Hub
          uses: docker/login-action@v4
          with:
           username: ${{ vars.DOCKERHUB_USERNAME }}
           password: ${{ secrets.DOCKERHUB_TOKEN }}

        - name: Set up QEMU
          uses: docker/setup-qemu-action@v4

        - name: Set up Docker Buildx
          uses: docker/setup-buildx-action@v4
        
        - name: Build and push Docker image
          run: |
            docker build -t carrebeanpirate/chaicode-node .
            docker push carrebeanpirate/chaicode-node
        
        - name: Deploy on cloud
          uses: appleboy/ssh-action@v1.0.3
          with:
            host: 210.79.128.114
            username: ubuntu
            key: ${{ secrets.PRIVATE_KEY }}
            script: |
              #kill running container
              #again pull fresh image
              #run container
              #sudo docker rm -f chaicode-node || true
              # sudo docker pull carrebeanpirate/chaicode-node
              #sudo docker run -d -p 3000:3000 --name chaicode-node carrebeanpirate/chaicode-node

              # clean up old images
              #sudo docker image prune -f

              # for caddy reverseProxy

              #CD /home/ubuntu/server/devops-test
              cd /home/ubuntu/server/devops-test

              #git pull
              git pull


              #make sure to pull fresh copy of carrebeanpirate/chaicode-node
              sudo docker pull carrebeanpirate/chaicode-node

              # run docker compose
              sudo docker compose up -d

              # clean up old images
              sudo docker image prune -f


          